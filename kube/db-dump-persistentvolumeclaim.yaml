apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: db-dump
  name: db-dump
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
